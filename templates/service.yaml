apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubia.fullname" . }}
  labels:
    app: {{ include "kubia.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "kubia.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
