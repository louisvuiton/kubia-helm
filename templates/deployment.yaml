apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kubia.fullname" . }}
  labels:
    app: {{ include "kubia.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "kubia.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "kubia.name" . }}
    spec:
      containers:
        - name: kubia
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
